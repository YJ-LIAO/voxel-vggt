# DEPRECATED: legacy heuristic-distillation TokenScorer training is disabled.
#
# Use the counterfactual-retention oracle pipeline instead:
#
# 1. Generate oracle shards:
#    /mnt/lyj/miniconda3/envs/streamvggt/bin/python tools/generate_counterfactual_oracle.py \
#      --mode collect-replay \
#      --replay-plan /path/to/replay_plan.pt \
#      --runner-factory your_package.your_runner:build_runner \
#      --output /path/to/mount/lyj/voxel-vggt/checkpoints/token_oracle/oracle_shard_000.pt
#
# 2. Train TokenScorer:
#    /mnt/lyj/miniconda3/envs/streamvggt/bin/python src/train_token_scorer_oracle.py \
#      --config config/train_token_scorer_oracle.yaml
#
# 3. Evaluate learned eviction:
#    /mnt/lyj/miniconda3/envs/streamvggt/bin/python tools/evaluate_learned_eviction.py \
#      --oracle-shards /path/to/mount/lyj/voxel-vggt/checkpoints/token_oracle/oracle_shard_000.pt \
#      --token-scorer-checkpoint /path/to/mount/lyj/voxel-vggt/checkpoints/token_scorer_oracle/token_scorer.pt
#
# The active training config is config/train_token_scorer_oracle.yaml.
redirect_config: config/train_token_scorer_oracle.yaml
training_script: src/train_token_scorer_oracle.py
reason: "TokenScorer must be trained from counterfactual retention oracle shards, not MLP-residual heuristic distillation."
